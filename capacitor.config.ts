import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prim_lek6_2022',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
